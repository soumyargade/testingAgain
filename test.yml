setup:
  - name: env
    steps:
      - name: env1
        run: 'echo "env1"'
  - name: file
    steps:
      - name: file1
        run: 'echo "file1"'
  - name: packages
    steps:
      - name: update
        run: 'apt-get update && apt-get upgrade -yqq'
      - name: Install pip and openjdk 11
        run: >
          apt-get install -yqq \
            python3-pip \
            openjdk-11-jdk

jobs:
  - name: Job1
    steps:
      - name: Step1
        run: 'echo "step1 running"'
      - name: Step2
        run: 'echo "step2 running"'
  - name: Job2
    steps:
      - name: Step3
        run: 'echo "step3 running"'
      - name: Step4
        run: 'echo "step4 running"'
